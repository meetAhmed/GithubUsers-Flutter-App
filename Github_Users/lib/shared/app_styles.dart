import 'package:flutter/material.dart';

// Holds constant TextStyles for different text elements in the app
class AppStyles {
  static const heading = TextStyle(fontWeight: FontWeight.bold, fontSize: 20.0);

  static const subHeading = TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 17.0,
  );

  static const normal = TextStyle(
    fontWeight: FontWeight.normal,
    fontSize: 15.0,
  );
}
